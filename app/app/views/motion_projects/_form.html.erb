<%= form_with(model: motion_project, local: true) do |form| %>
  <% if motion_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(motion_project.errors.count, "error") %> prohibited this motion_project from being saved:</h2>

      <ul>
      <% motion_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

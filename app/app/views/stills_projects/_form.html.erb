<%= form_with(model: stills_project, local: true) do |form| %>
  <% if stills_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stills_project.errors.count, "error") %> prohibited this stills_project from being saved:</h2>

      <ul>
      <% stills_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
